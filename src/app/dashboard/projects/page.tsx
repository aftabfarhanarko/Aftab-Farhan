import ProjectsManager from "@/components/Dashboard/ProjectsManager/ProjectsManager";
import React from "react";

const page = () => {
  return (
    <div>
      <ProjectsManager />
    </div>
  );
};

export default page;
